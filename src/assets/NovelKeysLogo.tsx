import Novelkeyslogo from "./novel-keys-logo.jpeg";
const NovelKeysLogo = (props: React.HTMLAttributes<HTMLImageElement>) => {
  return <img src={Novelkeyslogo} alt="NovelKeys logo" {...props} />;
};

export default NovelKeysLogo;
